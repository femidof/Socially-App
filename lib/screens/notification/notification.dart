import 'package:flutter/material.dart';

class NotificationChildScreen extends StatelessWidget {
  const NotificationChildScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
